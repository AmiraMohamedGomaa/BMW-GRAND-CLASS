<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amira.navigationdrawer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavigationDrawer"
        android:usesCleartextTraffic="true">
        <activity android:name="com.amira.splach.SplachActivity"
            android:theme="@style/Theme.NavigationDrawer.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity
            android:name=".ui.main_drawer.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.NavigationDrawer.NoActionBar">
        </activity>
        <activity android:name="com.amira.login.ui.LoginActivity"
            android:theme="@style/Theme.NavigationDrawer.NoActionBar">

        </activity>
    </application>

</manifest>